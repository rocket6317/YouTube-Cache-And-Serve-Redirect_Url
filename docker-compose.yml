version: "3.9"

services:
  youtube-redirector:
    build: .
    container_name: youtube-redirector
    ports:
      - "6095:6095"
    restart: unless-stopped
    environment:
      - YTDLP_EXTERNAL_JS=1
    volumes:
      - youtube_data:/app/data

volumes:
  youtube_data:
