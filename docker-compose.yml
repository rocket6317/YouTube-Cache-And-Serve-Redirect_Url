version: "3.9"

services:
  youtube-cache-serve:
    build: .
    container_name: youtube-cache-serve:
    ports:
      - "6095:6095"
    restart: unless-stopped
    environment:
      - YTDLP_EXTERNAL_JS=1
